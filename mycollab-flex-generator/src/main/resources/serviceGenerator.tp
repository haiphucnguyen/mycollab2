package $packageName
{
    <%importClasses.each { importClass -> %>
    import $importClass;
    <%}%>

    [RemoteClass(alias="$aliasClassName")]
    [Bindable]
    public class $className extends $superClassName
    { 
        
    }
}