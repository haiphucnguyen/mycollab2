$(window).load(function() {
	$('#slider').nivoSlider({
        pauseOnHover: false,
        pauseTime: 3000,
        onImageLoad: function(img) {
        },
        onImageChange: function(img) {
        }
      });
});