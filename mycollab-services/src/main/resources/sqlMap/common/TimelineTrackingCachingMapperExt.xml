<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.esofthead.mycollab.common.dao.TimelineTrackingCachingMapperExt">
    <sql id="queryTempSearchCriteria">
        <![CDATA[
        ]]>
    </sql>

    <select id="findTimelineItems" parameterType="map"
            resultMap="com.esofthead.mycollab.common.dao.GroupItemMapper.BaseResultMap" lang="velocity">
        #repeat(${_parameter.dates} $date "UNION" "" "")
            SELECT s_timeline_tracking_cache.fieldval AS groupid, s_timeline_tracking_cache.count AS value,
                DATE_FORMAT(@{date},"%Y-%m-%d") as groupname
            FROM s_timeline_tracking_cache
        #end
    </select>
</mapper>